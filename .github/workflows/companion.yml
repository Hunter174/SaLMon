name: Test salmon-model Companion

on:
  pull_request:
    paths:
      - 'tools/salmon-model/**'
      - '.github/workflows/companion.yml'
      - '.gitattributes'
  push:
    branches: [main, master]
    paths:
      - 'tools/salmon-model/**'
      - '.github/workflows/companion.yml'
      - '.gitattributes'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  companion:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        working-directory: tools/salmon-model
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          # Go 1.22's linker emits binaries rejected by current macOS runners
          # (missing LC_UUID). Keep the language baseline at 1.22, build with 1.24.
          go-version: '1.24.x'
          cache-dependency-path: tools/salmon-model/go.mod
      - name: Format check
        shell: bash
        run: test -z "$(gofmt -l .)"
      - name: Test
        run: go test ./...
      - name: Vet
        run: go vet ./...
      - name: Build standalone companion
        run: go build ./cmd/salmon-model
